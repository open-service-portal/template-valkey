---
# Crossplane Configuration Package metadata
# Defines how the template-valkey package is built and published.
apiVersion: meta.pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: configuration-valkey
  labels:
    provider: kubernetes
    type: database
    openportal.dev/template: "true"
spec:
  crossplane:
    version: ">=v2.0.0"
  dependsOn:
    - provider: xpkg.upbound.io/crossplane-contrib/provider-kubernetes
      version: ">=v0.14.0"
