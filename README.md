# template-valkey

Crossplane template for a self-service **Valkey** instance (`ValkeyInstance`, group `openportal.dev`). Backed by the official `valkey-io/valkey-operator`. See the spec in portal-workspace: `docs/specs/valkey-service.md`.
